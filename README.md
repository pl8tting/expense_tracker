# Expense Tracker
 
A simple CLI expense tracker I made using Python. Add expenses, view them, and check totals by category. It uses a local .json file so it stays between runs.
 
## Usage
```
python budget.py
```
